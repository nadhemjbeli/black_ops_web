{{ form_start(form, {
    attr:{
        novalidate:'novalidate'
    }
}) }}
    {{ form_row(form.nomSouscat) }}
    {{ form_row(form.idCat) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
