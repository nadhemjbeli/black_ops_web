<form method="post" action="{{ path('app_admin_image_delete', {'idImage': image.idImage}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image.idImage) }}">
    <button class="btn">Delete</button>
</form>
