<form method="post" action="{{ path('app_admin_champion_delete', {'idChamp': champion.idChamp}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ champion.idChamp) }}">
    <button class="btn">Delete</button>
</form>
