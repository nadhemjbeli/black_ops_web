<form method="post" action="{{ path('app_admin_video_uploade_delete', {'idVdeo': video_uploade.idVdeo}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ video_uploade.idVdeo) }}">
    <button class="btn">Delete</button>
</form>
