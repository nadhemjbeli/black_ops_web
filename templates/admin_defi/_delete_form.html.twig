<form method="post" action="{{ path('app_admin_defi_delete', {'idDefi': defi.idDefi}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ defi.idDefi) }}">
    <button class="btn ">Delete</button>
</form>
