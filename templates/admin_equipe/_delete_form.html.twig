<form method="post" action="{{ path('app_admin_equipe_delete', {'idEquipe': equipe.idEquipe}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipe.idEquipe) }}">
    <button class="btn btn-danger">Delete</button>
</form>
