{% extends 'Admin_base.html.twig' %}

{% block title %}Reclamation{% endblock %}

{% block content %}
    <h1>Reclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamation.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ reclamation.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reclamation.description }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ reclamation.etat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_reclamation_index') }}">back to list</a>

    <a href="{{ path('app_admin_reclamation_edit', {'id': reclamation.id}) }}">edit</a>

    {{ include('admin_reclamation/_delete_form.html.twig') }}
{% endblock %}
