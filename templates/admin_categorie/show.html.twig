{% extends 'Admin_base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block content %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCat</th>
                <td>{{ categorie.idCat }}</td>
            </tr>
            <tr>
                <th>NomCat</th>
                <td>{{ categorie.nomCat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_categorie_index') }}">back to list</a>

    <a href="{{ path('app_admin_categorie_edit', {'idCat': categorie.idCat}) }}">edit</a>

    {{ include('admin_categorie/_delete_form.html.twig') }}
{% endblock %}
