<form method="post" action="{{ path('app_contact_client_delete', {'idContact': contact.idContact}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact.idContact) }}">
    <button class="btn">Delete</button>
</form>
