<form method="post" action="{{ path('app_admin_jeu_delete', {'idJeu': jeu.idJeu}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jeu.idJeu) }}">
    <button class="btn">Delete</button>
</form>
