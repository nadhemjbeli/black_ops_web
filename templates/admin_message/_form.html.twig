{{ form_start(form, {
    attr:{
        novalidate:'novalidate'
    }
}) }}
    {{ form_row(form.contenuMessage,{
        attr:{
            'maxlength':'500',
            'ref':'message'
        }
    }) }}
    {{ form_row(form.idCl) }}
    {{ form_row(form.idSouscat) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
