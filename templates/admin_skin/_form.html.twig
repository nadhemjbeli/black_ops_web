{{ form_start(form , {
    attr:{
        'novalidate':'novalidate'
    }
}) }}

    {{ form_row(form.imageSkin ,{
        attr:{
            'maxlength':'50'
        }
    }) }}
    {{ form_row(form.idChamp) }}
    <button class="btn btn-outline-success">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
